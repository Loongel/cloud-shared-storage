# Stack 全场景测试报告 20260527-211347

## 结论

当前正式 Stack 测试链路已经能证明 hd01 单节点完整 36 场景通过；多节点 full 运行仍未达到交付通过标准。多节点失败集中在跨节点共享可见性和共享多写一致性，不应作为通过交付证据。

## 测试依据

- 测试计划：`docs/formal-stack-test-plan.md`
- 场景设计：`docs/scenario-test-design.md`
- 使用说明：`docs/scenario-test-guide.md`
- 场景矩阵：`deploy/scenario-test/scenarios.tsv`
- 正式测试入口：`scripts/css-scenario-test-deploy.sh`
- 批量测试入口：`scripts/css-scenario-test-batch.sh`

## 已有报告摘要

| 报告目录 | Profile | 节点 | 场景 | PASS | FAIL | BLOCKED | 结论 |
| --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| `reports/css-scenario-css-hd01-smoke-20260527-delivery` | `smoke` | `hd01` | 4 | 4 | 0 | 0 | 通过 |
| `reports/css-scenario-css-hd01-core-20260527-delivery` | `core` | `hd01` | 18 | 18 | 0 | 0 | 通过 |
| `reports/css-scenario-css-hd01-backup-20260527-delivery` | `backup-only` | `hd01` | 18 | 18 | 0 | 0 | 通过 |
| `reports/css-scenario-css-hd01-full-20260527-delivery` | `full` | `hd01` | 36 | 36 | 0 | 0 | 通过 |
| `reports/css-scenario-css-allnodes-full-20260527-delivery` | `full` | `hd01,ora01,ora02,wawo01` | 39 | 24 | 15 | 0 | 未通过 |

## 多节点失败摘要

多节点 full 报告中的失败如下：

| 类型 | 失败项 |
| --- | --- |
| 私有单写后端校验 | `p_s_a_p_n` 在 `ora01` 后端 direct check 缺失 |
| 共享单写可见性 | `s_s_a_p_n`、`s_s_a_e_n` 在 `ora01` 未读到 writer 标记 |
| 共享多写文件可见性 | `s_m_a_*`、`s_m_st_*` 只看到 `hd01`，缺少其他节点标记 |
| 共享多写 SQLite | `s_m_sq_*` 出现 `sqlite_row_count_mismatch` |

这些失败说明多节点共享写/读链路仍需继续修复，不能降级为网络层负载均衡或单节点通过。

## 安装和升级状态

- 一键安装脚本默认解析 GitHub latest Release 的 deb 包；显式 `CSS_RELEASE_VERSION`、`--release-version` 或 `--deb-url` 时才固定版本。
- `cs-storage-auto-upgrade.timer` 当前保持开发期 5s 周期。
- 当前 GitHub latest Release 为 `v0.1.32`，包含 auto-upgrade curl 超时修复和 `TimeoutStartSec=900s`。
- 当前 hd01 证据显示 `cs-storage-auto-upgrade.timer` 已启用且最近运行成功；本机已通过 5s timer 自动升级到 `cs-storage 0.1.32`，三个 CSS systemd 服务保持 active/enabled。

## 清理记录

- 已删除本地构建产物：`bin/`、`dist/`、根目录 `cs-storage-server`。
- 已删除异常误写文件：`%s,*`。
- 已删除旧容器化 CSS runtime stack：`deploy/stack/cs-storage-daemon-global.yml`、`deploy/stack/cs-storage-plugin-global.yml`、`deploy/stack/cs-storage-server.yml`。
- 保留历史 `reports/` 目录，作为问题追踪和交付证据来源。
- 保留冒烟测试脚本；历史 Swarm host-mutation helper 默认禁用，避免再次跨节点修改主机状态。

## 下一步

1. 用新的 batch 入口重新跑低并发多节点 smoke/core，避免一次性 full 放大 Swarm overlay 抖动。
2. 先修复多节点共享单写读取缺失，再修复共享多写文件标记缺失。
3. 对 `shared+multi+sqlite` 单独做多节点 SQLite 写入与 backend 下载校验，修复 row count mismatch。
4. 多节点 full 必须达到 `FAIL=0`、`BLOCKED=0` 后才能作为最终交付通过报告。
